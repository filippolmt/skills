# Local graph-analysis artifacts (regenerated by a machine-local git hook)
graphify-out/

# mode-router handoff notes: work in progress being carried across a /clear
.mode-router/

# wayfinder: mappa e ticket locali (non versionati)
.wayfinder/

# Per-machine Claude Code settings: never tracked
.claude/settings.local.json
